# Web (Dockerfile.web) build context. Keep it small + exclude the other runtimes
# so the SPA image only ships what Vite needs. (The backend builds from its own
# backend/ context, so this file does not affect the backend image.)
node_modules
dist
dist-ops
.git
.github
backend
technician-app
.venv
**/__pycache__
**/*.pyc
docs
*.log
.vscode
.idea
coverage
.DS_Store
